extends layout

block content
  h1= title
  p Welcome to #{title}. Learning pug .
  button(type="button", class="btn btn-secondary" onclick="update()") Press Me for contents
   p
  div(id ="target") Display

  script.
    var client_count=0;
    function update(){
      client_count+=1;
      
      document.getElementById("target").innerHTML="Value Now "+client_count  ;
    }

  p 
  a(href="iphone") Link to the page iphone